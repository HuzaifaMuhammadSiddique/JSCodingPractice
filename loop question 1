// Calculate the sum of even numbers from 1 to a given number.

// CODE 
let num = parseInt(prompt("Enter the number: "));
var sum = 0;

for(let i = 2; i <= num; i+=2){
    sum+= i;
}

console.log(sum);
