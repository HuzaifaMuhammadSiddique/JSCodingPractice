// Reverse a string.

// CODE
let string = prompt("Enter a word: "); // level
let l = string.length // 5
var Rstring = "";

for(let i=l-1; i>=0; i--){
    Rstring = Rstring + string[i];
}

console.log(Rstring);
