/*
Leap Year Checker: Write a program that prompts the user to enter a year and determines whether it is a leap year or not. A leap year
is divisible by 4, but not divisible by 100, unless it is also divisible by 400.
*/

// CODE

let year = prompt("Enter a year: ");
year = Number.parseInt(year);

if (year % 4 === 0 && (year % 100 !== 0 || year % 400 === 0)){
    alert("It is a leap year"); 
}else {
    alert("It is not a leap year");
}
